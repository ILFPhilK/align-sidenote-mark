# History Notes

This repository was assembled from the plugin versions created during the ChatGPT conversation.

- Tags `v0.7.0` through `v0.12.0` are based on the actual plugin packages available in the working runtime.
- Tags `v0.1.0` through `v0.6.0` are reconstructed from the conversation's version descriptions and update requests so the repository has a continuous history from the initial version.
- The latest production-ready version is `v0.12.0`.

